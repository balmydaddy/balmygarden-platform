# dependencies
node_modules/
.pnp
.pnp.js

# build
.next/
out/
build/
dist/

# env (절대 커밋하지 마세요 — API 키 포함)
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

.vercel
.env*.local

# Claude Code 에이전트 격리 작업공간(중첩 worktree) — 소스 아님, 커밋 대상 아님
# (디렉터리 자체가 아니라 내용물을 무시해야 아래 예외가 먹는다 — git은 제외된
#  디렉터리 안쪽 파일은 다시 포함시키지 못함)
.claude/*
# 단, 도구 권한 설정은 세션마다 다시 묻지 않도록 저장소에 남긴다
# (클라우드 세션은 컨테이너가 매번 새로 떠서 사용자 홈에 두면 사라짐)
!.claude/settings.json
