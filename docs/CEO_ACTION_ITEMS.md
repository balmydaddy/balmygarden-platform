# CEO 액션 항목

> 작성: 2026-08-20 (CONDUCTOR)
> 이 문서는 **CEO만 할 수 있는 일**만 담는다. AI가 대신할 수 있는 건 여기 없다.
> 각 항목에 "왜 CEO여야 하는지"를 같이 적었다.

---

## 0. 지금 막힌 것 — 최우선

### 0-1. Anthropic 조직 월 사용한도 초과 ⛔

**증상**: 코드 리뷰 에이전트 2건이 실행 도중 죽었다.
> `You've hit your org's monthly spend limit`

**영향**: LOD-DEV-03·PLAT-01 자동 개발 루프의 **3단계(분리 리뷰)가 돌지 않는다.**
구현·CI는 정상이지만 "생성 세션과 분리된 별도 Agent 리뷰 95/100"을 못 받아
사이클을 닫을 수 없다. 지금 코드 3건이 리뷰 대기로 멈춰 있다.

**CEO 조치**: https://claude.ai/settings/usage 에서 한도 상향
(관리자 권한 필요 — AI가 접근 불가)

**한도 상향 전까지의 상태**: 구현·CI·타입체크·빌드는 계속 돌릴 수 있다.
리뷰만 못 돈다. 무리하게 자기 검토로 통과 처리하지 않는다(AEGIS 원칙).

---

### 0-2. MCP 서버 인증 필요 (경미)

서버 `72dd7fd2-...`가 미인증 상태다. 이 세션은 비대화형이라 OAuth를 못 돈다.
필요하면 claude.ai 커넥터 설정에서 인증. 지금 작업에 막힌 건 없다.

---

## 1. LOD 게임 — 로컬 PC에서만 가능

> Unity 빌드·Play 테스트는 CEO 로컬 PC(`D:\lord-of-dark`)에서만 된다.
> 클라우드 세션은 그 PC에 접근할 수 없다. 그래서 이 항목들이 CEO 몫이다.

### 1-1. `git pull` 후 로비 화면 확인 — **Build All Scenes 불필요**

```
cd D:\lord-of-dark
git pull
```

최신 커밋 `455fb1e` (CI 그린, run 30). 씬 파일을 안 건드리고 런타임 생성
방식이라 재빌드 없이 바로 보인다.

**확인할 것**
- [ ] 로비 상단 배너: `2026년 대한민국 · 균열 발생 구역 — 각성자 대기소`
- [ ] 배너가 아래 직업 정보 패널을 **안 덮는지** (겹침이 이번 사이클 수정 대상이었다)
- [ ] 스테이지 진입 버튼 라벨이 **"균열 진입"** 인지 (기존 "전투 시작")
- [ ] 골드·다이아 표시가 배너와 안 겹치는지

### 1-2. 로비 배경 이미지 재생성 — **클라우드에서 불가**

```
cd D:\lord-of-dark
python3 tools/generate_lobby_background.py
```

**왜 CEO 로컬인가**: 프롬프트는 이미 "아르카나 대륙 고대 요새" → "현대 도심
재개발 부지"로 고쳤지만(커밋 `4310789`), 저장소에 커밋된 PNG는 **아직 옛
프롬프트 산출물**이다. 클라우드에서 pollinations.ai 접근이 막혀 있다
(HTTP 000 실측 확인). 로컬에서만 재생성된다.

- [ ] 스크립트 실행
- [ ] 로비 배경이 현대 도심으로 바뀌었는지 확인 후 커밋·push

### 1-3. 밀려 있는 Play 테스트 확인 항목

이전 사이클들이 코드·CI는 통과했지만 **시각적 동작은 미확정**인 것들이다.
정적 리뷰와 CI는 컴파일까지만 본다 — 실제로 보이는지는 사람이 눌러봐야 한다.

- [ ] 전투 중 파티클 이펙트 (주황/흰=일반, 노랑/빨강=크리티컬, 초록=힐, 보라=페이즈전환)
- [ ] mage 스킬 목록에 파이어볼·가이디드애로우·풀스톤·매직홀 / cleric에 큐어
- [ ] 콘솔에 `Setting the duration...` 에러가 더는 안 뜨는지
- [ ] orc/vampire/dragon 처치 시 반지·팔찌·벨트 드롭
- [ ] 체감 난이도 (위 수정들 반영 후 재평가하기로 CEO가 이미 결정한 사안)

### 1-4. Unity CLI — 명령어 목록 공유

설치한 Unity CLI(v1.0.0-beta.5)로 `Build All Scenes`를 자동화하려면
사용 가능한 명령을 알아야 한다.

- [ ] 로컬에서 `unity help` 실행 후 **출력 전체**를 붙여주기

---

## 2. 플랫폼 (balmygarden-platform) — 배포본에서만 확인 가능

> 로컬 세션엔 프로덕션 키가 없어 실제 Notion·트레이딩 응답을 검증할 수 없다.

### 2-1. 자물쇠 재인증 — **한 번은 반드시 해야 함**

이번에 잠금 방식을 바꿨다. 기존에 잠금해제해둔 브라우저는 **서버 쿠키가
없어서** 홈 화면의 미결 수치·업무 로그·트레이딩이 안 뜬다.

- [ ] 배포본 접속 → 우측 상단 **🔓 자물쇠 클릭(= 잠그기)** → 다시 클릭 → 비밀번호 입력
- [ ] 홈 화면에 미결 사항 수치(총/완료/진행/미진행)가 뜨는지
- [ ] 업무 로그에 담당 에이전트 배지와 함께 항목이 뜨는지

**안 뜬다면**: Notion 통합이 "BALMYGARDEN — 미결 사항" 페이지에 공유돼 있는지 확인

### 2-2. Vercel 환경변수 확인

이번 보안 작업이 이 값들에 의존한다. 하나라도 없으면 조용히 잠긴 것처럼 보인다.

| 변수 | 없으면 생기는 일 |
|---|---|
| `DASHBOARD_UNLOCK_PASSWORD` | 잠금해제가 아예 불가 — 모든 보호 API가 401 |
| `CRON_SECRET` | **cron·SCOUT 자동화가 통째로 401** (이번에 내부 호출 토큰으로도 씀) |
| `NOTION_API_KEY` | 미결 수치·업무 로그 안 뜸 |
| `TRADING_SERVER_URL` | 트레이딩 오프라인 (터널 안 쓰면 정상 상태) |

- [ ] Vercel 대시보드에서 4개 존재 확인 (특히 `CRON_SECRET`)

### 2-3. 자동화 정상 동작 확인 (다음 cron 실행 후)

`POST /api/notion`·`/api/naver-news`에 인증을 걸었다. 내부 호출엔 헤더를
붙였지만 **실제 프로덕션에서 도는 건 배포 후에만 확인된다.**

- [ ] 내일 Notion에 SCOUT 뉴스 브리핑·일일 업무 로그가 평소대로 쌓이는지
- [ ] 안 쌓이면 알려주기 → 즉시 롤백 가능

---

## 3. 음악 / 마케팅

### 3-1. Instagram 캡션 오타 3건 — **API로 수정 불가**

Buffer API는 이미 게시된 글을 수정하지 못한다. IG 앱에서 직접 고쳐야 한다.

| 잘못 나간 것 | 고칠 것 |
|---|---|
| 이어폰 **꺼는** 순간 | 이어폰 **꽂는** 순간 |
| **전곁은** | **전곡은** |
| **#신곱** | **#신곡** |

- [ ] IG 앱에서 해당 게시물 캡션 수정

내 실수다. 한글을 유니코드 이스케이프로 손수 인코딩해 올리면서 게시 전
재검토를 안 했다 — CLAUDE.md 문서 검토 원칙을 어긴 것.

---

## 4. 안전관리 (WARDEN) — 본인 계정 필요

### 4-1. Notion 템플릿 마켓플레이스 등록

위험성평가 체크리스트의 Notion 템플릿판 1차 빌드는 끝났다
(허브 페이지 `3bb987a2-5d61-81e9-a28e-e2a090cf8ad2`).

- [ ] 마켓플레이스 크리에이터 등록 (본인 계정 필요)
- [ ] 가격 책정 (마켓플레이스 최소 판매가 정책 확인 필요)
- [ ] 리스팅 초안 최종 검토 후 게시

---

## 5. GOSARI 음악 — **AI 대체 불가, 22일 지연 중**

### 5-1. Bridge v0.1 P-13 재독 🔴 P0

**GOSARI 트랙 전체가 이 한 건에 멈춰 있다.** Bridge → Verse 1 → Verse 2 →
Intro → Outro → 리듬 검토 → QG → Suno까지 전부 대기 상태다.

P-13 재독은 "시간을 두고 다시 읽었을 때 감정이 살아 있는가"를 보는 절차라
사람만 할 수 있다. AI 판정으로 대체하면 게이트 자체가 무의미해진다.

- [ ] Bridge v0.1 재독 후 결과 알려주기 (PASS/FAIL + 걸리는 문장)

---

## 6. 외부 계정 — 확인만 하면 되는 것

### 6-1. GCP 결제 계정 해지 확인 🔴

결제 계정 `013672-2264B4-987674`가 2026-08-12 해지됐다. 의도한 게 아니면
연결된 프로젝트의 청구 서비스(VM 등)가 중지된다. 클라우드 세션은 GCP
콘솔에 접근할 수 없다.

- [ ] 의도한 해지인지 확인 → 아니면 Google Cloud Console에서 재활성화

### 6-2. Anthropic 보안 알림 — 신규 Cowork 원격기기 🔴

2026-08-12 계정에 새 Cowork 원격 기기가 추가됐다.

- [ ] 본인 기기가 맞는지 확인 → 아니면 즉시 계정 보안 조치

### 6-3. 네이버 검색 API 키 (선택)

SCOUT 뉴스 브리핑이 라우트는 있는데 키가 없어 실호출 검증이 안 됐다.

- [ ] 개발자센터 또는 NCP API HUB에서 키 발급 → Vercel 환경변수 등록

---

## 우선순위 요약

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | 0-1 Anthropic 한도 상향 | 자동 개발 루프의 리뷰 단계가 멈춰 있음 |
| 2 | 5-1 Bridge P-13 재독 | GOSARI 전체가 22일째 정지 |
| 3 | 6-1 GCP / 6-2 보안 알림 | 방치 시 서비스 중지·보안 위험 |
| 4 | 2-1 자물쇠 재인증 + 2-2 환경변수 | 안 하면 대시보드가 빈 화면으로 보임 |
| 5 | 1-1 로비 확인 / 1-2 배경 재생성 | LOD 사이클 마무리 |
| 6 | 3-1 IG 오타 | 이미 노출 중 |
| 7 | 4-1 Notion 마켓 / 1-4 Unity CLI / 6-3 네이버 키 | 신규 판로·자동화 확장 |

---

## 참고 — 지금 코드 상태

| 저장소 | 최신 커밋 | CI | 리뷰 |
|---|---|---|---|
| lord-of-dark | `455fb1e` | ✅ success (run 30) | ⏸ 한도 초과로 대기 |
| balmygarden-platform | `b9ef81b` | tsc·build ✅ | ⏸ 한도 초과로 대기 |

**리뷰 대기 = 배포 금지가 아니다.** 이미 push됐고 Vercel은 자동 배포된다.
다만 "95/100 분리 리뷰 통과"라는 게이트를 아직 못 받았으므로 **완료로
보고하지 않는다.** 한도가 풀리면 리뷰부터 돌린다.
